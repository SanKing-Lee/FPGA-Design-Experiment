CWD = $(shell pwd)

TOPLEVEL_LANG = verilog

VERILOG_SOURCES = $(CWD)/../src/ex1_dual_trig_sync.v

MODULE = test_dual_trig_sync
TOPLEVEL = dual_trig_sync 

include $(shell cocotb-config --makefiles)/Makefile.sim
CWD = $(shell pwd)

TOPLEVEL_LANG = verilog

VERILOG_SOURCES = $(CWD)/../../src/ex1_edge_detect_sync.v

MODULE = test_edge_detect_sync
TOPLEVEL = edge_detect_sync

include $(shell cocotb-config --makefiles)/Makefile.sim